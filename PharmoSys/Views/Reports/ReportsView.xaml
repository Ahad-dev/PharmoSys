<UserControl x:Class="PharmoSys.Views.Reports.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:vm="clr-namespace:PharmoSys.ViewModels.Reports"
             Background="#F5F6FA" FontFamily="Segoe UI">

    <UserControl.DataContext>
        <vm:ReportsViewModel/>
    </UserControl.DataContext>

    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" Padding="20,14" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="📈 Sales Reports" Style="{StaticResource Heading2}" Foreground="{StaticResource PrimaryBrush}"/>
                    <TextBlock Text="Analyze revenue and top selling products over time." Style="{StaticResource Subtitle}" Margin="0,2,0,0"/>
                </StackPanel>

                <!-- Filters -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="From:" VerticalAlignment="Center" Margin="0,0,5,0" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                    <DatePicker SelectedDate="{Binding StartDate}" Width="110" Margin="0,0,15,0"/>
                    
                    <TextBlock Text="To:" VerticalAlignment="Center" Margin="0,0,5,0" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                    <DatePicker SelectedDate="{Binding EndDate}" Width="110" Margin="0,0,15,0"/>

                    <Button Content="↻ Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource OutlineButton}" Margin="0,0,10,0"/>
                            
                    <Button Content="📄 Export PDF" Command="{Binding ExportPdfCommand}" Style="{StaticResource DangerButton}" Margin="0,0,10,0"/>
                            
                    <Button Content="📊 Export Excel" Command="{Binding ExportExcelCommand}" Style="{StaticResource AccentButton}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Top Metrics Cards -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource ModernCard}" Padding="20">
                <StackPanel>
                    <TextBlock Text="Total Revenue" Style="{StaticResource Subtitle}" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TotalRevenue, StringFormat='PKR {0:N0}'}" FontSize="28" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource ModernCard}" Padding="20">
                <StackPanel>
                    <TextBlock Text="Total Orders" Style="{StaticResource Subtitle}" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding TotalSalesCount}" FontSize="28" FontWeight="Bold" Foreground="{StaticResource InfoBrush}" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Chart and Top Products -->
        <Grid Grid.Row="2" Margin="20,0,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Chart -->
            <Border Grid.Column="0" Style="{StaticResource ModernCard}" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Revenue Trend" Style="{StaticResource Heading2}" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,15"/>
                    <lvc:CartesianChart Grid.Row="1" Series="{Binding SalesChartSeries}" XAxes="{Binding XAxes}"/>
                </Grid>
            </Border>

            <!-- Top Products -->
            <Border Grid.Column="2" Style="{StaticResource ModernCard}" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Top Selling Products" Style="{StaticResource Heading2}" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,15"/>
                    
                    <DataGrid Grid.Row="1" ItemsSource="{Binding TopProducts}" Style="{StaticResource ModernDataGrid}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Product" Binding="{Binding ProductName}" Width="*"/>
                            <DataGridTextColumn Header="Sold" Binding="{Binding TotalSold}" Width="60">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
