<UserControl x:Class="PharmoSys.Views.Products.ProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900"
             FontFamily="Segoe UI">

    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}" Padding="20,14" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="💊 Product Management" Style="{StaticResource Heading2}" Foreground="{StaticResource PrimaryBrush}"/>
                    <TextBlock Text="Add, edit, and manage all medicines and inventory." Style="{StaticResource Subtitle}" Margin="0,2,0,0"/>
                </StackPanel>

                <!-- Action Buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="＋ Add Product" Command="{Binding AddProductCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,8,0"/>
                    <Button Content="✏ Edit" Command="{Binding EditProductCommand}" Style="{StaticResource InfoButton}" Margin="0,0,8,0"/>
                    <Button Content="🗑 Delete" Command="{Binding DeleteProductCommand}" Style="{StaticResource DangerButton}" Margin="0,0,8,0"/>
                    <Button Content="↻ Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource OutlineButton}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Search Bar -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" Padding="20,10" Margin="0,10,0,0">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource ModernTextBox}"
                     Width="320"
                     HorizontalAlignment="Left"
                     ToolTip="Search products by name or category"/>
        </Border>

        <!-- Product DataGrid -->
        <Border Grid.Row="2" Margin="0,10,0,0" CornerRadius="10" ClipToBounds="True">
            <DataGrid ItemsSource="{Binding Products}"
                      SelectedItem="{Binding SelectedProduct}"
                      Style="{StaticResource ModernDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID"       Binding="{Binding ProductId}" Width="50"/>
                    <DataGridTextColumn Header="Name"     Binding="{Binding Name}" Width="180"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="110"/>

                    <!-- Price with teal color -->
                    <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat='PKR {0:N2}'}" Width="110">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#16A085"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Stock Quantity badge -->
                    <DataGridTemplateColumn Header="Stock" Width="70">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="12" Padding="10,3" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2,0">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#D5F5E3"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsLowStock}" Value="True">
                                                    <Setter Property="Background" Value="#FADBD8"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding StockQuantity}" FontWeight="Bold" FontSize="12">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#1E8449"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsLowStock}" Value="True">
                                                        <Setter Property="Foreground" Value="#C0392B"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Stock Status badge -->
                    <DataGridTemplateColumn Header="Stock Status" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="12" Padding="10,3" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="2,0">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#D5F5E3"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding StockStatus}" Value="Low Stock">
                                                    <Setter Property="Background" Value="#FADBD8"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding StockStatus}" Value="Out of Stock">
                                                    <Setter Property="Background" Value="#F9EBEA"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding StockStatus}" FontWeight="SemiBold" FontSize="11">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#1E8449"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding StockStatus}" Value="Low Stock">
                                                        <Setter Property="Foreground" Value="#C0392B"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding StockStatus}" Value="Out of Stock">
                                                        <Setter Property="Foreground" Value="#922B21"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Expiry Date" Binding="{Binding ExpiryDate, StringFormat='dd/MM/yyyy'}" Width="110"/>

                    <!-- Expiry Status badge -->
                    <DataGridTemplateColumn Header="Expiry Status" Width="130">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="12" Padding="10,3" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="2,0">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#D5F5E3"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ExpiryStatus}" Value="Expiring Soon">
                                                    <Setter Property="Background" Value="#FEF9E7"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ExpiryStatus}" Value="Expired">
                                                    <Setter Property="Background" Value="#FADBD8"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding ExpiryStatus}" FontWeight="SemiBold" FontSize="11">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#1E8449"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ExpiryStatus}" Value="Expiring Soon">
                                                        <Setter Property="Foreground" Value="#9A7D0A"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ExpiryStatus}" Value="Expired">
                                                        <Setter Property="Foreground" Value="#C0392B"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Supplier" Binding="{Binding SupplierName}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>

