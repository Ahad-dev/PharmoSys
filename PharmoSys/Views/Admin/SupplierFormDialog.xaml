<Window x:Class="PharmoSys.Views.Admin.SupplierFormDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Supplier" Height="300" Width="400" WindowStartupLocation="CenterScreen" Background="#F5F6FA" FontFamily="Segoe UI">
    <Border Background="{StaticResource SurfaceBrush}" Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Supplier Name:" Grid.Row="0" Style="{StaticResource Subtitle}" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="txtName" Grid.Row="1" Style="{StaticResource ModernTextBox}" Margin="0,0,0,15"/>

            <TextBlock Text="Contact Info:" Grid.Row="2" Style="{StaticResource Subtitle}" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="txtContact" Grid.Row="3" Style="{StaticResource ModernTextBox}" Margin="0,0,0,15"/>

            <TextBlock Text="Address:" Grid.Row="4" Style="{StaticResource Subtitle}" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="txtAddress" Grid.Row="5" Style="{StaticResource ModernTextBox}" Margin="0,0,0,25"/>

            <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Click="BtnCancel_Click" Style="{StaticResource OutlineButton}" Margin="0,0,10,0" Padding="20,10"/>
                <Button Content="Save" Click="BtnSave_Click" Style="{StaticResource PrimaryButton}" Padding="20,10"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
