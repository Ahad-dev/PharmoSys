<UserControl x:Class="PharmoSys.Views.Products.ProductView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900"
             FontFamily="Segoe UI">

    <Grid Background="#F5F6FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" Padding="20,14" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="💊 Product Management" FontSize="22" FontWeight="Bold" Foreground="#16A085"/>
                    <TextBlock Text="Add, edit, and manage all medicines and inventory." FontSize="13" Foreground="#7F8C8D" Margin="0,2,0,0"/>
                </StackPanel>

                <!-- Action Buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="＋ Add Product" Command="{Binding AddProductCommand}"
                            Background="#16A085" Foreground="White"
                            Padding="16,8" Margin="0,0,8,0" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                    <Button Content="✏ Edit" Command="{Binding EditProductCommand}"
                            Background="#2980B9" Foreground="White"
                            Padding="16,8" Margin="0,0,8,0" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                    <Button Content="🗑 Delete" Command="{Binding DeleteProductCommand}"
                            Background="#E74C3C" Foreground="White"
                            Padding="16,8" Margin="0,0,8,0" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                    <Button Content="↻ Refresh" Command="{Binding RefreshCommand}"
                            Background="#95A5A6" Foreground="White"
                            Padding="16,8" FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Search Bar -->
        <Border Grid.Row="1" Background="White" Padding="20,10" Margin="0,10,0,0">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Padding="10,8"
                     FontSize="14"
                     BorderBrush="#BDC3C7"
                     Width="320"
                     HorizontalAlignment="Left">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TextBox">
                                    <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="4" Background="White">
                                        <Grid>
                                            <TextBlock Text="🔍 Search by name or category..." Foreground="#BDC3C7" Padding="10,8"
                                                       Visibility="{Binding Text, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </Border>

        <!-- Product DataGrid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Products}"
                  SelectedItem="{Binding SelectedProduct}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  BorderThickness="0"
                  Background="White"
                  RowHeight="40"
                  Margin="0,10,0,0"
                  AlternatingRowBackground="#F9FFFE">

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#16A085"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="FontSize" Value="13"/>
                    <Setter Property="Padding" Value="12,8"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"       Binding="{Binding ProductId}" Width="50"/>
                <DataGridTextColumn Header="Name"     Binding="{Binding Name}" Width="180"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="110"/>
                <DataGridTextColumn Header="Price"    Binding="{Binding Price, StringFormat='PKR {0:N2}'}" Width="110"/>
                <DataGridTextColumn Header="Stock"    Binding="{Binding StockQuantity}" Width="70"/>
                <DataGridTextColumn Header="Stock Status"  Binding="{Binding StockStatus}" Width="110"/>
                <DataGridTextColumn Header="Expiry Date"   Binding="{Binding ExpiryDate, StringFormat='dd/MM/yyyy'}" Width="110"/>
                <DataGridTextColumn Header="Expiry Status" Binding="{Binding ExpiryStatus}" Width="130"/>
                <DataGridTextColumn Header="Supplier"      Binding="{Binding SupplierName}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>

