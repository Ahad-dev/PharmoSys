<Window x:Class="PharmoSys.Views.Admin.UserFormDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="User" Height="360" Width="400" WindowStartupLocation="CenterScreen" Background="#F5F6FA" FontFamily="Segoe UI">
    <Border Background="{StaticResource SurfaceBrush}" Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Username:" Grid.Row="0" Style="{StaticResource Subtitle}" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="txtUsername" Grid.Row="1" Style="{StaticResource ModernTextBox}" Margin="0,0,0,15"/>

            <TextBlock Text="Full Name:" Grid.Row="2" Style="{StaticResource Subtitle}" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="txtFullName" Grid.Row="3" Style="{StaticResource ModernTextBox}" Margin="0,0,0,15"/>

            <TextBlock Text="Phone:" Grid.Row="4" Style="{StaticResource Subtitle}" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="txtPhone" Grid.Row="5" Style="{StaticResource ModernTextBox}" Margin="0,0,0,15"/>

            <TextBlock Text="Role (1=Admin, 2=Cashier, 3=Manager):" Grid.Row="6" Style="{StaticResource Subtitle}" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <TextBox x:Name="txtRoleId" Grid.Row="7" Style="{StaticResource ModernTextBox}" Margin="0,0,0,15"/>

            <TextBlock Text="Password:" Grid.Row="8" Style="{StaticResource Subtitle}" FontWeight="SemiBold" Margin="0,0,0,5"/>
            <PasswordBox x:Name="txtPassword" Grid.Row="9" Margin="0,0,0,25"/>

            <StackPanel Grid.Row="10" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Click="BtnCancel_Click" Style="{StaticResource OutlineButton}" Margin="0,0,10,0" Padding="20,10"/>
                <Button Content="Save" Click="BtnSave_Click" Style="{StaticResource PrimaryButton}" Padding="20,10"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
