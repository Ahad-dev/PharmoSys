<Window x:Class="PharmoSys.Views.Auth.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PharmoSys.Views.Auth"
        mc:Ignorable="d"
        Title="PharmoSys Login" Height="400" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="30">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Welcome to PharmoSys" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            
            <TextBlock Text="Username" Margin="0,5,0,2"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15" Padding="5"/>

            <TextBlock Text="Password" Margin="0,5,0,2"/>
            <!-- Note: PasswordBox doesn't support binding directly. We update ViewModel via PasswordChanged event in Code Behind -->
            <PasswordBox x:Name="txtPassword" PasswordChanged="txtPassword_PasswordChanged" Margin="0,0,0,15" Padding="5"/>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Center" Margin="0,0,0,10" TextWrapping="Wrap"/>

            <Button Content="LOGIN" Command="{Binding LoginCommand}" Background="#2C3E50" Foreground="White" Height="35" FontWeight="Bold"/>

            <TextBlock Text="Loading..." Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" HorizontalAlignment="Center" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>
