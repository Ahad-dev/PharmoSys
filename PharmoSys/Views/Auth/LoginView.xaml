<Window x:Class="PharmoSys.Views.Auth.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PharmoSys.Views.Auth"
        mc:Ignorable="d"
        Title="PharmoSys Login" Height="400" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Border Background="{StaticResource SurfaceBrush}">
        <Grid Margin="40">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Welcome to PharmoSys" Style="{StaticResource Heading1}" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,30"/>
                
                <TextBlock Text="Username" Style="{StaticResource Subtitle}" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}" Margin="0,0,0,20"/>

                <TextBlock Text="Password" Style="{StaticResource Subtitle}" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <!-- Note: PasswordBox doesn't support binding directly. We update ViewModel via PasswordChanged event in Code Behind -->
                <PasswordBox x:Name="txtPassword" PasswordChanged="txtPassword_PasswordChanged" Margin="0,0,0,20" Padding="10"/>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" HorizontalAlignment="Center" Margin="0,0,0,15" TextWrapping="Wrap"/>

                <Button Content="LOGIN" Command="{Binding LoginCommand}" Style="{StaticResource PrimaryButton}" Height="45" Margin="0,10,0,0"/>

                <TextBlock Text="Loading..." Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,15,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
